.$(tab) {
  @apply list-none;
  @apply m-0;
  @apply flex;
  @apply items-center;
  @apply flex-wrap;

  & &-item {
    @apply mt-0;
    @apply p-2;
    @apply relative;
    @apply no-underline;
    @apply outline-none;

    a {
      @apply p-2;
      @apply no-underline;
    }

    &[disabled],
    &:disabled,
    &.disabled {
      @apply cursor-default;
      @apply pointer-events-none;
      @apply text-gray-400;
    }
  }
}