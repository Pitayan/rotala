.$(breadcrumb) {
  @apply list-none;
  @apply m-0;
  @apply p-0;

  .$(breadcrumb)-item {
    @apply inline-block;
    @apply m-0;

    a {
      @apply no-underline;
    }

    &.active {
      @apply no-underline;
    }
  }
}