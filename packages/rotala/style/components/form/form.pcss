.$(form-group) {
  &-fieldset {
    @apply mb-2;
  }

  &-legend {
    @apply text-lg;
    @apply font-normal;
    @apply mb-2;
  }

  &-item {
    &:not(:last-child) {
      @apply mb-2;
    }
  }

  &-label {
    @apply block;
    @apply leading-normal;
    @apply py-1;
  }
}
