
.$(link) {
  @apply transition-all;
  @apply ease-in-out;
  @apply duration-150;
  @apply no-underline;

  &:focus {
    @apply shadow-primary-base;
  }

  &:hover,
  &:active,
  &.active {
    @apply text-primary-500;
  }
}
