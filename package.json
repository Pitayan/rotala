{
  "name": "rotalacss",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna publish",
    "publish-canary": "lerna publish --canary",
    "prepare": "lerna run prepare"
  }
}
